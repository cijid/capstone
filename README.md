# capstone
Team B Capstone

## Installation

Create a .env file in the root directory based on the .env.example file and fill out the variables with your preferences.

```
docker-compose up --build -d && docker-compose exec api npm run db:seed
```